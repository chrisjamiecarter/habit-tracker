@page
@model HabitTracker.WebUI.Pages.DeleteHabitLogModel
@{
    ViewData["Title"] = "Delete Habit Log";
}

<h2>Delete TODO HabitName Logs</h2>
<hr />
<h3>Are you sure you want to delete this record?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.HabitLog!.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(x => x.HabitLog!.Date)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.HabitLog!.Quantity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(x => x.HabitLog!.Quantity)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="HabitLog!.Id" />
        <input type="hidden" asp-for="HabitLog!.HabitId" />
        <input type="submit" value="Delete" class="btn btn-danger" />
    </form>
</div>

<div>
    <a asp-page="ViewHabitLogs" asp-route-habitId="@Model.HabitLog!.HabitId">Back</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
